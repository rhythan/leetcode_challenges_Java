class Solution {
    public int mirrorDistance(int n) {
        return Math.abs(n - reverse(n));
    }

    int reverse(int n) {
        int ans = 0;
        while (n > 0) {
            int x = n % 10;
            ans = ans * 10 + x;
            n /= 10;
        }
        return ans;
    }
}
