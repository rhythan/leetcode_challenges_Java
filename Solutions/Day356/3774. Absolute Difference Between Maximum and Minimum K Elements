class Solution {
    public int absDifference(int[] nums, int k) {
        Arrays.sort(nums);
        int Lsum = 0 , Msum = 0 ;
        for(int i = 0 ; i < k ; i ++){
            Msum+=nums[i];
            Lsum+=nums[nums.length - k + i];
        }
        return Math.abs(Msum-Lsum);
    }
}
