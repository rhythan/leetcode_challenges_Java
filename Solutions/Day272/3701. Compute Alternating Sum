class Solution {
    public int alternatingSum(int[] nums) {
        int sum = 0, x = 1;

        for(int num : nums){
            sum += x * num;
            x *= -1;
        }

        return sum;
    }
}
